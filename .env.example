# KhanijDrishti environment configuration (example).
# The frontend prototype runs with zero required env vars (all data is local/demo).
# These are placeholders for the production roadmap — never commit real secrets.

# App
NEXT_TELEMETRY_DISABLED=1
NEXT_PUBLIC_APP_ENV=demo            # demo | staging | production
NEXT_PUBLIC_DEMO_MODE=true          # never imply live connectivity when true

# --- Roadmap: backend / data sources (unused by the frontend prototype) ---
# API_BASE_URL=http://api:8000
# DATABASE_URL=postgresql+psycopg://khanij:change-me@postgres:5432/khanijdrishti
# REDIS_URL=redis://redis:6379/0
# OPENALEX_MAILTO=
# CROSSREF_MAILTO=
# Authorised patent/R&D connector credentials go here (kept out of version control).
